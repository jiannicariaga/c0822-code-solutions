/* exported isLowerCased */

// 1) Check if string input matches the all-lowercase version of itself
// 2) Output (boolean) result

function isLowerCased(word) {
  return word === word.toLowerCase();
}
