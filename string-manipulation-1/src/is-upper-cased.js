/* exported isUpperCased */

// 1) Check if string input matches the all-uppercase version of itself
// 2) Output (boolean) result

function isUpperCased(word) {
  return word === word.toUpperCase();
}
